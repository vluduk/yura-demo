export const environment = {
    // Backend API base used by the frontend during development.
    // Pointing to Django dev server (monolith) at port 8080 and include API prefix.
    serverURL: "http://localhost:8080/api/v1",
};
