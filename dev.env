# =============================================================================
# YURA Development Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Django Settings
# -----------------------------------------------------------------------------
DEBUG=True
SECRET_KEY=AIzaSyCTKi3xlbeaeHlwS0_fzE1UmIFdbaUNI-c

# -----------------------------------------------------------------------------
# Database Configuration (PostgreSQL)
# -----------------------------------------------------------------------------
# Database name
DATABASE_NAME=yura_db
POSTGRES_DB=yura_db

# Database user
DATABASE_USER=yura_user
POSTGRES_USER=yura_user

# Database password
DATABASE_PASSWORD=yura_password
POSTGRES_PASSWORD=yura_password

# Database host (use 'db' for docker-compose service name)
DATABASE_HOST=db
POSTGRES_HOST=db

# Database port
DATABASE_PORT=5432
POSTGRES_PORT=5432

# -----------------------------------------------------------------------------
# Superuser Configuration
# -----------------------------------------------------------------------------
DJANGO_SUPERUSER_EMAIL=admin@yura.com
DJANGO_SUPERUSER_PASSWORD=admin123


# -----------------------------------------------------------------------------
# Google AI / LLM Configuration
# -----------------------------------------------------------------------------
# Google API Key for Gemini
# IMPORTANT: Replace 'your-google-api-key-here' with your actual API key
GOOGLE_API_KEY=AIzaSyDqP1sYWoguLAYVo9sRe-Prc3hz4epCaWc

# Google LLM Model to use
GOOGLE_LLM_MODEL=models/gemini-2.5-flash

# System prompt for the AI assistant (in Ukrainian)
GOOGLE_LLM_SYSTEM_PROMPT=Ви — корисний, ввічливий та уважний кар'єрний радник для українських ветеранів. Відповідайте українською мовою, стисло та по суті. Якщо потрібно, ставте уточнюючі запитання, щоб краще зрозуміти користувача.

# -----------------------------------------------------------------------------
# JWT Authentication Configuration
# -----------------------------------------------------------------------------
# Access token lifetime in minutes
JWT_ACCESS_TOKEN_LIFETIME_MINUTES=15

# Refresh token lifetime in days
JWT_REFRESH_TOKEN_LIFETIME_DAYS=7

# JWT signing key (should be different from Django SECRET_KEY in production)
JWT_SECRET_KEY=jwt-secret-key-change-in-production

# JWT algorithm
JWT_ALGORITHM=HS256

# -----------------------------------------------------------------------------
# CORS & CSRF Configuration
# -----------------------------------------------------------------------------
# Allowed origins for CORS (comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:4200,http://127.0.0.1:4200,http://localhost:4201,http://127.0.0.1:4201,http://localhost:8080

# Trusted origins for CSRF (comma-separated)
CSRF_TRUSTED_ORIGINS=http://localhost:4200,http://localhost:4201,http://localhost:8080

# -----------------------------------------------------------------------------
# Logging Configuration
# -----------------------------------------------------------------------------
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# Additional Settings
# -----------------------------------------------------------------------------
# Default page size for pagination
DEFAULT_PAGE_SIZE=20
